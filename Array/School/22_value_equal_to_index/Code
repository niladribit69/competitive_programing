class Solution{
public:

	vector<int> valueEqualToIndex(int arr[], int n) {
	    // code here
	    vector<int> v1;
	    for(int i=0;i<n;i++){
	        if(arr[i]==i+1){
	            v1.push_back(i+1);
	        }
	      
	    }
	    return v1;
	}
};
